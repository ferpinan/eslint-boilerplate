# eslint-boilerplate

A simple boilerplate with the basic configuration of Eslint. Just a boilerplate to play with its config and lear about this library.

## Install

```shell
npm install
```

## Run

Run the hello worl app:

```shell
npm start
```

Run Eslint

```shell
npm run eslint
```